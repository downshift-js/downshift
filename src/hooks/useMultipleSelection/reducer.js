export default function downshiftMultipleSelectionReducer(state, action) {
  const {type} = action
  let changes

  switch (type) {
    default:
      break
  }

  return {
    ...state,
    ...changes,
  }
}
